# Tasks: Подключение фронтенда к API дилемм

**Input**: Design documents from `specs/001-connect-backend/`  
**Prerequisites**: `plan.md`, `spec.md`, `research.md`, `data-model.md`, `contracts/`, `quickstart.md`

**Tests**: Явные тестовые задачи не добавляю (TDD не запрошен явно), но каждая фаза содержит критерии независимой проверки.

**Organization**: Задачи сгруппированы по user stories, чтобы каждая история могла реализовываться и проверяться независимо.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Можно делать параллельно (разные файлы, нет жёсткой зависимости).  
- **[Story]**: User story — `[US1]`, `[US2]`, `[US3]`.  
- В описании всегда есть конкретный путь к файлу/директории.

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Базовая настройка окружения и конфигурации API для фронтенда.

- [x] T001 Обновить документацию по переменной `VITE_API_BASE_URL` (файл `frontend-dilemma/README.md`) с примерами значений для локального и Railway API  
- [x] T002 [P] Добавить в `frontend-dilemma/README.md` явную ссылку на `specs/001-connect-backend/quickstart.md`

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Общая инфраструктура и базовые абстракции, без которых невозможно реализовать ни одну user story.

- [x] T003 Создать или доработать модуль API‑клиента в `frontend-dilemma/src/shared/lib/api.ts` с поддержкой `VITE_API_BASE_URL` и базовых HTTP‑методов
- [x] T004 [P] Реализовать в `frontend-dilemma/src/shared/lib/api.ts` автоматическое добавление заголовка `X-User-UUID` во все запросы для мутаций (initial/final choice)
- [x] T005 [P] Добавить в `frontend-dilemma/src/shared/lib/api.ts` централизованную обработку ошибок (HTTP/сеть) и типизированный формат ошибки для UI (ApiError с типами network/http/parse)
- [x] T006 Создать util/хук генерации и хранения UUID пользователя в `frontend-dilemma/src/shared/hooks/useClientUuid.ts` с хранением в `localStorage`
- [x] T007 [P] Привязать `useClientUuid` (через `getClientUuid`) к API‑клиенту в `frontend-dilemma/src/shared/lib/api.ts`, чтобы UUID использовался прозрачно для всех вызовов, требующих идентификации пользователя

**Checkpoint**: Базовый клиент для API настроен, UUID пользователя стабилен, ошибки приходят в едином формате — можно реализовывать user stories.

---

## Phase 3: User Story 1 - Прохождение дилеммы с живыми данными (Priority: P1) 🎯 MVP

**Goal**: Пользователь может пройти одну дилему целиком (список → детали → initial choice → фидбэк → final choice) с использованием живого backend‑API.

**Independent Test**: Поднять backend и frontend по `quickstart.md`, пройти одну дилему от начала до конца и убедиться, что все шаги используют реальные данные и сохраняются на сервере.

### Implementation for User Story 1

- [x] T008 [P] [US1] Добавить функции `fetchDilemmas` и `fetchDilemmaDetails` (аналог `fetchDilemmaByName`) в `frontend-dilemma/src/shared/lib/api.ts` с вызовами `/api/dilemmas` и `/api/dilemmas/{name}`
- [x] T009 [P] [US1] Расширить существующий контекст/хук дилем в `frontend-dilemma/src/app/context/DilemmaContext.ts` и `DilemmaProvider.tsx` для загрузки списка дилем через `fetchDilemmas` с состоянием загрузки и ошибок
- [x] T010 [US1] Обновить страницу выбора дилем `frontend-dilemma/src/pages/DilemmaSelectionPage.tsx`, чтобы она использовала данные из контекста (dilemmas, isLoadingDilemmas, dilemmasError) вместо локальных констант
- [x] T011 [P] [US1] Добавить функцию `submitInitialChoice` в `frontend-dilemma/src/shared/lib/api.ts` для вызова `/api/decisions/initial` и получения фидбэка (BackendFeedbackResponse)
- [x] T012 [US1] Обновить страницу первоначального выбора `frontend-dilemma/src/pages/ChoicePage.tsx`, чтобы она отправляла initial choice через `submitInitialChoice` и отображала фидбэк‑текст из ответа сервера с обработкой ошибок
- [x] T013 [P] [US1] Добавить функцию `submitFinalChoice` в `frontend-dilemma/src/shared/lib/api.ts` для вызова `/api/decisions/final`
- [x] T014 [US1] Обновить страницу финального решения `frontend-dilemma/src/pages/ReasonPage.tsx`, чтобы она отправляла final choice через `submitFinalChoice` и после успешного ответа обновляла состояние дилемы
- [x] T015 [US1] Обеспечить, чтобы после перезагрузки страницы информация о пройденной дилеме и принятых решениях загружалась с backend‑API (минимум для текущей дилеммы)

**Checkpoint**: Одна полная дилема проходит end‑to‑end на живом API, это минимальный MVP.

---

## Phase 4: User Story 2 - Согласованность состояния между фронтендом и сервером (Priority: P1)

**Goal**: При повторном заходе или на другом устройстве пользователь видит на фронтенде данные, соответствующие серверным (история и статистика).

**Independent Test**: Пройти дилему, затем открыть приложение в новом браузере/инкогнито (с тем же UUID, если возможно) и убедиться, что история и статусы дилем подгружаются с сервера и совпадают с ожидаемыми.

### Implementation for User Story 2

- [x] T016 [P] [US2] Добавить функцию `fetchMyDecisions` в `frontend-dilemma/src/shared/lib/api.ts` для вызова `/api/decisions/my`
- [x] T017 [US2] Добавить/расширить хук `useDilemmaData` в `frontend-dilemma/src/shared/hooks/useDilemmaData.ts`, чтобы он умел загружать историю решений пользователя через `fetchMyDecisions`
- [x] T018 [P] [US2] Реализовать маппинг истории решений в признак `isCompletedByUser` для каждой дилемы в контексте `frontend-dilemma/src/app/context/DilemmaContext.ts`
- [x] T019 [US2] Обновить UI списка дилем в `frontend-dilemma/src/pages/DilemmaSelectionPage.tsx`, чтобы отображать, какие дилемы уже пройдены/на какой стадии находится пользователь
- [x] T020 [P] [US2] Добавить функцию `fetchDilemmaStats` в `frontend-dilemma/src/shared/lib/api.ts` для вызова `/api/statistics/dilemma/{name}`
- [x] T021 [US2] Обновить страницу статистики `frontend-dilemma/src/pages/StatsPage.tsx`, чтобы она загружала и отображала статистику по текущей дилеме с backend‑API вместо локальных заглушек (с обработкой загрузки и ошибок)
- [x] T022 [US2] Убедиться, что при монтировании основных страниц (выбор дилем, статистика, инсайты) фронтенд всегда рефрешит данные с сервера, а не полагается на устаревшее локальное состояние

**Checkpoint**: История и статистика полностью берутся с сервера, UI соответствует серверным данным даже после повторных заходов.

---

## Phase 5: User Story 3 - Устойчивое поведение при сбоях API (Priority: P2)

**Goal**: При недоступности API, ошибках 4xx/5xx или сетевых проблемах пользователь видит понятные сообщения и может повторить действия.

**Independent Test**: Сымитировать ошибки (отключить backend, вернуть 500/404 через dev‑инструменты) и проверить, что фронтенд корректно отображает сообщения и остаётся работоспособным.

### Implementation for User Story 3

- [x] T023 [P] [US3] Расширить централизованную обработку ошибок в `frontend-dilemma/src/shared/lib/api.ts`, чтобы возвращать типизированный объект ошибки (код/тип/сообщение для i18n)
- [x] T024 [US3] Обновить `frontend-dilemma/src/shared/hooks/useDilemmaData.ts`, чтобы он сохранял ошибки загрузки списка дилем и позволял триггерить повторный запрос
- [x] T025 [P] [US3] Обновить `frontend-dilemma/src/pages/DilemmaSelectionPage.tsx`, чтобы она отображала локализованные сообщения об ошибке при падении `/api/dilemmas` и кнопку "повторить"
- [x] T026 [P] [US3] Обновить `frontend-dilemma/src/pages/ChoicePage.tsx`, чтобы при ошибках initial choice показывались понятные сообщения и была возможность отправить выбор повторно
- [x] T027 [US3] Обновить страницы финального решения/инсайтов (`frontend-dilemma/src/pages/ReasonPage.tsx`, `InsightPage.tsx`), чтобы при ошибках final choice или статистики UI вёл себя предсказуемо (информативные сообщения, повторные попытки, отсутствие "подвешенного" состояния)

**Checkpoint**: Основные сценарии деградации (нет списка дилем, ошибка initial/final choice, проблемы со статистикой) обрабатываются контролируемо и не "ломают" приложение.

---

## Phase N: Polish & Cross-Cutting Concerns

**Purpose**: Улучшения, которые затрагивают несколько user stories.

- [x] T028 [P] Перепроверить все тексты ошибок и уведомлений в `frontend-dilemma/src/shared/i18n/locales/*/translation.json` на предмет консистентности и понятности
- [x] T029 Пройтись по основным страницам (`frontend-dilemma/src/pages/*.tsx`) и удалить оставшиеся зависимости от локальных заглушечных данных, если таковые ещё остались
- [x] T030 [P] Обновить `frontend-dilemma/src/shared/lib/utils.ts` (если используется) для форматирования статистики/процентов так, чтобы это совпадало с ожиданиями UI и серверными данными
- [x] T031 Запустить сценарий из `specs/001-connect-backend/quickstart.md` целиком и задокументировать найденные отличия/баги в отдельном разделе `Known Issues` в `frontend-dilemma/README.md`

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: Нет зависимостей — можно начинать сразу.  
- **Foundational (Phase 2)**: Зависит от Setup — блокирует все user stories, пока не настроен API‑клиент и UUID.  
- **User Stories (Phase 3–5)**: Все зависят от Foundational; US1 и US2 — приоритет P1, US3 — P2.  
- **Polish (Final Phase)**: Зависит от завершения основных user stories (минимум US1, желательно US2–US3).

### User Story Dependencies

- **User Story 1 (P1)**: Можно начинать сразу после Phase 2; не зависит от других историй.  
- **User Story 2 (P1)**: Требует Phase 2 и базовую реализацию US1 (минимум — получение дилем и initial/final choice).  
- **User Story 3 (P2)**: Требует Phase 2 и минимум базовую реализацию всех основных API‑вызовов (US1/US2), т.к. оборачивает их обработкой ошибок.

### Within Each User Story

- Сначала API‑вызовы и хуки, затем обновление контекста/состояния, затем правки страниц/компонентов.  
- После завершения истории — ручная проверка по Acceptance Scenarios из `spec.md`.

### Parallel Opportunities

- T002, T004, T005, T007 можно выполнять параллельно (разные аспекты конфигурации/клиента).  
- Внутри US1, US2 и US3 задачи с маркером `[P]` могут выполняться параллельно разными разработчиками (работа с API‑слоем и страницами).  
- Разные user stories (US1 и US2) можно вести параллельно после завершения Phase 2, если есть несколько исполнителей.

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Выполнить Phase 1 и Phase 2.  
2. Реализовать Phase 3 (US1) до чекпоинта.  
3. Прогнать сценарии из US1 вручную против живого API (Railway или локальный backend).  
4. Зафиксировать MVP‑состояние (деплой/демо).

### Incremental Delivery

1. После MVP (US1) добавить Phase 4 (US2) для согласованности состояния и статистики.  
2. Затем Phase 5 (US3) для устойчивости при сбоях.  
3. Завершить фичу Phase N (Polish), убрав оставшиеся заглушки и подчистив UX/копирайты.


